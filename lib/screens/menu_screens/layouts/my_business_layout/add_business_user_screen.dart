import '../../../../config.dart';

class AddBusinessUserScreen extends StatelessWidget {
  const AddBusinessUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBarCommon(title: "Business User"));
  }
}
